# QtMenuBar
Custom QT QML menu bar. The menu bar takes a ListModel for data.

## Building the project

```bash
# From the project root directory, run:
cmake -S . -B build -G "Ninja"
ninja -C build
```
